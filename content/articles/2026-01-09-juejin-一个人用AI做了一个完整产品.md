# 一个人用AI，7天做了一个完整产品：技术栈与实践复盘

> AI超人成长记 | 超级个体实验
>
> 关键词：AI辅助开发、Claude、Next.js、全栈开发、超级个体

---

## 前言

说出来你可能不信，我一个人，用AI，7天做了一个完整的产品。前端、后端、UI设计、测试、文档，全是我一个人。

这不是炫技，而是想分享一下：2026年了，AI辅助开发到底能做到什么程度？

本文会从技术角度复盘这7天的开发过程，包括技术选型、AI协作方式、踩过的坑，以及一些实用的Prompt技巧。

---

## 一、项目概述

### 技术栈

| 层级 | 技术选型 |
|-----|---------|
| 前端 | Next.js 14 (App Router) + TypeScript |
| 样式 | Tailwind CSS + Shadcn/UI |
| 后端 | Go (API服务) |
| AI模型 | Claude Opus 4.5 |
| 部署 | Vercel + 自建服务器 |

### 为什么选这套技术栈？

1. **Next.js**：SSR/SSG灵活切换，App Router的Server Components对SEO友好
2. **Tailwind CSS**：AI生成的代码可读性强，不用来回切换CSS文件
3. **Go**：编译后单文件部署，性能好，适合API服务
4. **Claude**：代码生成质量高，上下文理解能力强

---

## 二、AI协作的正确姿势

### 2.1 需求分析阶段

一开始我只有一个模糊的想法。直接丢给Claude：

```
我想做一个xxx平台，目标用户是xxx，你帮我分析一下：
1. 核心功能有哪些？
2. MVP版本应该包含什么？
3. 技术上有什么建议？
```

Claude直接给出了：
- 功能优先级矩阵
- 用户故事列表
- 技术架构建议
- 甚至还有数据库表设计的初稿

**关键点**：不要一上来就让AI写代码，先让它帮你理清思路。

### 2.2 代码生成阶段

#### 好的Prompt示例

```
帮我写一个用户认证模块，要求：
- 使用Next.js 14 App Router
- 支持邮箱登录
- 使用JWT token
- 包含登录、注册、登出功能
- 使用TypeScript，类型要完整
```

#### 不好的Prompt示例

```
帮我写个登录功能
```

**差别在哪？** 好的Prompt包含了：
1. 技术栈约束
2. 功能范围
3. 具体要求
4. 质量标准

### 2.3 Debug阶段

AI Debug的效率是真的高。我的常用Prompt：

```
这段代码报错了：[粘贴错误信息]

代码如下：[粘贴代码]

请分析原因并给出修复方案。
```

Claude不仅能找到Bug，还会解释为什么会出这个问题，以及如何避免类似问题。

---

## 三、实际代码示例

### 3.1 API路由示例

```typescript
// app/api/chat/route.ts
import { NextRequest, NextResponse } from 'next/server';

export async function POST(request: NextRequest) {
  try {
    const { message } = await request.json();

    // 调用后端API
    const response = await fetch(`${process.env.API_URL}/chat`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ message }),
    });

    if (!response.ok) {
      throw new Error('API request failed');
    }

    const data = await response.json();
    return NextResponse.json(data);

  } catch (error) {
    return NextResponse.json(
      { error: 'Internal server error' },
      { status: 500 }
    );
  }
}
```

这段代码是AI生成的，我只做了少量调整。

### 3.2 组件示例

```tsx
// components/ChatInput.tsx
'use client';

import { useState } from 'react';
import { Button } from '@/components/ui/button';
import { Textarea } from '@/components/ui/textarea';

interface ChatInputProps {
  onSend: (message: string) => void;
  disabled?: boolean;
}

export function ChatInput({ onSend, disabled }: ChatInputProps) {
  const [input, setInput] = useState('');

  const handleSubmit = () => {
    if (!input.trim()) return;
    onSend(input);
    setInput('');
  };

  return (
    <div className="flex gap-2">
      <Textarea
        value={input}
        onChange={(e) => setInput(e.target.value)}
        placeholder="输入消息..."
        disabled={disabled}
        onKeyDown={(e) => {
          if (e.key === 'Enter' && !e.shiftKey) {
            e.preventDefault();
            handleSubmit();
          }
        }}
      />
      <Button onClick={handleSubmit} disabled={disabled || !input.trim()}>
        发送
      </Button>
    </div>
  );
}
```

---

## 四、踩过的坑

### 4.1 AI生成的代码不能直接用

AI生成的代码大概率能跑，但不一定符合你的项目规范。需要：
- 检查命名是否一致
- 检查错误处理是否完善
- 检查类型定义是否准确

### 4.2 上下文丢失问题

对话太长，AI会"忘记"之前的约定。解决方案：
- 重要约定放在System Prompt
- 定期总结当前进度
- 复杂功能拆分成多个小任务

### 4.3 AI的"自信"陷阱

AI有时候会很自信地给出错误答案。特别是：
- 最新的API变更（训练数据截止问题）
- 特定版本的Breaking Changes
- 边界情况的处理

**建议**：关键逻辑一定要自己验证。

---

## 五、效率提升数据

| 任务 | 传统方式 | AI辅助 | 提升 |
|-----|---------|-------|------|
| 需求分析 | 2天 | 2小时 | 8x |
| UI开发 | 3天 | 1天 | 3x |
| API开发 | 2天 | 0.5天 | 4x |
| 文档编写 | 1天 | 1小时 | 8x |
| Debug | 随机 | 减少50% | 2x |

**总体效率提升：约3-5倍**

---

## 六、总结

### 超级个体的本质

不是你什么都会，而是：
- **会提问**：知道问AI什么问题
- **会拆解**：把大目标拆成小任务
- **会执行**：别光想，动手干
- **会验证**：AI的输出要自己把关

### AI辅助开发的边界

AI能做的：
- 快速生成样板代码
- 解释复杂概念
- Debug和代码审查
- 文档生成

AI做不好的：
- 复杂的架构决策
- 业务逻辑的深度理解
- 性能优化的细节
- 安全相关的关键代码

---

## 🎁 福利

文章里用的Claude Opus 4.5，官方订阅要$200/月。但我做了一个工具，可以**免费使用**这个顶级模型。

- 程序员：可以接入Cursor、Claude Code等开发工具
- 普通用户：直接对话使用

想要的朋友，评论区留言或私信我，发你安装包和使用教程。

---

**关注「AI Superman DJY」，一起用AI成为超级个体**

如果觉得有用，欢迎点赞、收藏、分享！

---

## 配图提示词

### 图1：封面图 - 技术架构
**中文描述**：一个开发者面前展示着技术架构图，包含Next.js、Go、Claude等技术栈图标，科技感
**英文提示词**：A developer looking at a floating tech architecture diagram with Next.js, Go, Claude logos, futuristic holographic display, blue and purple gradient, flat illustration style, dark background
**风格参数**：--ar 16:9

### 图2：AI协作流程
**中文描述**：人与AI对话的流程图，展示需求->代码->测试->部署的循环
**英文提示词**：A flowchart showing human-AI collaboration cycle: requirements, coding, testing, deployment, with a person and AI robot working together, flat illustration, blue purple theme, clean design
**风格参数**：--ar 16:9

### 图3：效率对比
**中文描述**：左右对比图，左边传统开发（多人团队、长时间），右边AI辅助（一个人、快速）
**英文提示词**：Split comparison image, left: traditional team development with multiple people and long timeline, right: single developer with AI assistant and short timeline, flat illustration, blue purple gradient
**风格参数**：--ar 16:9
